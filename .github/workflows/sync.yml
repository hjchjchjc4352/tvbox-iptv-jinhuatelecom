name: Sync and Rewrite IPTV

on:
  schedule:
    - cron: "0 5 * * 1"   # 每周一执行
  workflow_dispatch:      # 允许手动执行

jobs:
  sync:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout your repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # --- 步骤 1：下载新项目的 TXT 直播源 ---
      - name: Download upstream IPTV file
        run: |
          curl -L "https://raw.githubusercontent.com/LionixQ/Zhejiang_Telecom_IPTV/main/Zhejiang_Unicast/Zhejiang_Unicast.txt" \
            -o upstream.txt
      
      # --- 步骤 2：下载新项目对应带 Logo 的 M3U 文件 ---
      - name: Download upstream IPTV logo m3u
        run: |
          curl -L "https://raw.githubusercontent.com/LionixQ/Zhejiang_Telecom_IPTV/main/Zhejiang_Unicast/Zhejiang_Unicast_OL.m3u" \
            -o Hangzhou_Telecom_Unicast_ONLINE_LOGO.m3u

      - name: Run rewrite script
        run: python rewrite.py

      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions@github.com"
          git config --local user.name "GitHub Actions"
          git add jinhuatelecomiptv.txt jinhuatelecomiptv.m3u oneiptv.txt oneiptv.m3u
          if [ -n "$(git status --porcelain)" ]; then
            git commit -m "Auto sync & rewrite IPTV"
            git push
          else
            echo "No changes to commit"
          fi
